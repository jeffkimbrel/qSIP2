# Calculate the delta EAF statistics

Calculate the delta EAF statistics

## Usage

``` r
get_delta_stats(delta_distributions, confidence = 0.95)
```

## Arguments

- delta_distributions:

  a tibble

- confidence:

  (numeric, default: 0.95) confidence interval

## Value

a tibble of statistics

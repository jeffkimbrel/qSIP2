# Plot resampling convergence (under construction!)

Plot resampling convergence (under construction!)

## Usage

``` r
plot_resampling_convergence(qsip_data_object)
```

## Arguments

- qsip_data_object:

  (*qsip_data*) A qsip data object that has been resampled

# Validate a qsip object has been filtered

Validate a qsip object has been filtered

## Usage

``` r
is_qsip_filtered(object, error = FALSE)
```

## Arguments

- object:

  The object to check if it is a filtered qsip_data object

- error:

  If TRUE it stops with an error message. If FALSE it doesn't error, but
  returns FALSE

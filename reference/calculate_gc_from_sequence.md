# Calculate GC% from a sequence

This function takes a sequence and counts the number of G and C bases,
and divides by the total sequence length.

## Usage

``` r
calculate_gc_from_sequence(sequence)
```

## Arguments

- sequence:

  (*string*) Density or WAD values

## Value

A vector of GC% values

# Seed used in resampling

Returns the seed used in the resampling step, or `NULL` if no specific
seed was given.

## Usage

``` r
resample_seed(qsip_data_object)
```

## Arguments

- qsip_data_object:

  A `qSIP_data` or list of `qSIP_data` objects.

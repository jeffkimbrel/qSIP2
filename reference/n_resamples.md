# Number of resamples

This function returns the number of resamples that were performed on the
data. It will return `NULL` if the data has not been resampled.

## Usage

``` r
n_resamples(qsip_data_object)
```

## Arguments

- qsip_data_object:

  A `qSIP_data` object or list of objects

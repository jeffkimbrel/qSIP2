# Check object is qsip_data type

Check object is qsip_data type

## Usage

``` r
is_qsip_data(object, error = FALSE)
```

## Arguments

- object:

  The object to check if it is a qsip_data object

- error:

  If TRUE it stops with an error message. If FALSE it doesn't error, but
  returns FALSE

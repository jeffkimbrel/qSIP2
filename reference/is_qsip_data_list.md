# Validate a multi-qsip list object

Validate a multi-qsip list object

## Usage

``` r
is_qsip_data_list(object, error = FALSE)
```

## Arguments

- object:

  A list of qsip_data objects

- error:

  If TRUE it stops with an error message. If FALSE it doesn't error, but
  returns FALSE

# Create a method for source_wads

Calculate global weighted average density (WAD) value for all
source_mat_id (internal) accounts for data that is not filtered or
already filtered

## Value

A dataframe with two columns, 1) the source_mat_id and 2) the global WAD
value for that source_mat_id
